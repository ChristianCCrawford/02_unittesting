#For unit test
import unittest
import pytest
from MODULE import add, sub, sum

#Check if a is added 
def check_add(a):
   """ Add number to itself """
   assert een < a
   
   print(MODULE.add())

#Check if a is now = to 0 
def check_sub(a):
   """ Number subtract by itself equaling 0 """
   assert ren == 0
   
   print(MODULE.sub())
   
#Check if sum is = to a   
def check_sum(a):
   """ Sum should be the exact same number """ 
   assert ten == a 
   
   print(MODULE.sum())